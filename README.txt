--- Changes ---

The autoencoder takes the latent dimension as well as the input dim to utilize the added 'tools/dimension_estimator.py' to determine the bottleneck. Also, the AE requires the 'build' method be called for the layers to be generated.
